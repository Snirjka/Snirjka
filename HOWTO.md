delete assets, static, index.html and service-worker.
change static-dev folder name to static.
run npm run build.
rename main static folder to static-dev.
extract build folder to main.
Add, commit and push!.